{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/message-event/2.1.0/message-event.json",
  "title": "MessageEvent",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "message_id", "role", "created_at", "content"],
  "properties": {
    "type": {
      "const": "message",
      "description": "Event discriminator."
    },
    "message_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for this message event."
    },
    "role": {
      "type": "string",
      "enum": ["user", "assistant", "system"],
      "description": "Who authored the message."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp."
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier for the request that produced this message."
    },
    "response_to": {
      "type": "string",
      "minLength": 1,
      "description": "message_id that this message responds to."
    },
    "content": {
      "$ref": "#/$defs/MessageContent"
    },
    "_meta": {
      "$ref": "#/$defs/Meta",
      "description": "Metadata for observability. Fields: client_msg_id, request_id, trace_id, model, tokens."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "content": {
            "properties": {
              "items": {
                "contains": {
                  "type": "object",
                  "properties": { "type": { "const": "tool_result" } },
                  "required": ["type"]
                }
              }
            },
            "required": ["items"]
          }
        },
        "required": ["content"]
      },
      "then": {
        "properties": {
          "content": {
            "properties": {
              "items": {
                "contains": {
                  "type": "object",
                  "properties": { "type": { "const": "tool_call" } },
                  "required": ["type"]
                }
              }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "MessageContent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["items"],
      "properties": {
        "items": {
          "type": "array",
          "minItems": 1,
          "description": "Ordered message items. Array order is the render order.",
          "items": { "$ref": "#/$defs/Item" }
        }
      }
    },

    "Item": {
      "description": "A single renderable or non-renderable item within a message.",
      "oneOf": [
        { "$ref": "#/$defs/TextItem" },
        { "$ref": "#/$defs/FileItem" },
        { "$ref": "#/$defs/ToolCallItem" },
        { "$ref": "#/$defs/ToolResultItem" }
      ]
    },

    "TextItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "renderable", "data"],
      "properties": {
        "type": { "const": "text", "description": "Text item." },
        "renderable": {
          "const": true,
          "description": "Whether this item should be rendered to end users."
        },
        "data": { "$ref": "#/$defs/TextData" }
      }
    },

    "FileItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "renderable", "data"],
      "properties": {
        "type": { "const": "file", "description": "Generic file item." },
        "renderable": { "const": true, "description": "Whether this item should be rendered to end users." },
        "data": { "$ref": "#/$defs/FileData" }
      }
    },


    "ToolCallItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "renderable", "data"],
      "properties": {
        "type": { "const": "tool_call", "description": "Non-renderable tool invocation." },
        "renderable": {
          "const": false,
          "description": "Whether this item should be rendered to end users."
        },
        "data": { "$ref": "#/$defs/ToolCallData" }
      }
    },

    "ToolResultItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "renderable", "data"],
      "properties": {
        "type": { "const": "tool_result", "description": "Non-renderable tool execution result." },
        "renderable": {
          "const": false,
          "description": "Whether this item should be rendered to end users."
        },
        "data": { "$ref": "#/$defs/ToolResultData" }
      }
    },

    "TextData": {
      "type": "object",
      "additionalProperties": false,
      "required": ["text"],
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "maxLength": 100000,
          "description": "Plain text content."
        }
      }
    },

    "FileData": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "media_type", "uri", "filename"],
      "properties": {
        "source": {
          "type": "string",
          "enum": ["uri", "base64"],
          "description": "How the file bytes are provided."
        },
        "media_type": {
          "type": "string",
          "minLength": 3,
          "description": "IANA media type (MIME type)."
        },
        "uri": {
          "type": "string",
          "description": "If source=uri, an absolute URI. If source=base64, the base64 payload without a data: prefix."
        },
        "filename": {
          "type": "string",
          "minLength": 1,
          "maxLength": 512,
          "description": "Original file name (for display/download)."
        },
        "bytes": {
          "type": "integer",
          "minimum": 0,
          "description": "File size in bytes (if known)."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "source": { "const": "uri" } },
            "required": ["source"]
          },
          "then": {
            "properties": {
              "uri": {
                "format": "uri",
                "pattern": "^[A-Za-z][A-Za-z0-9+.-]*:\\/\\/.+$"
              }
            }
          }
        },
        {
          "if": {
            "properties": { "source": { "const": "base64" } },
            "required": ["source"]
          },
          "then": {
            "properties": {
              "uri": {
                "pattern": "^(?:[A-Za-z0-9+\\/]{4})*(?:[A-Za-z0-9+\\/]{2}==|[A-Za-z0-9+\\/]{3}=)?$",
                "contentEncoding": "base64"
              }
            }
          }
        }
      ]
    },

    "ToolCallData": {
      "type": "object",
      "additionalProperties": false,
      "required": ["call_id", "name", "args"],
      "properties": {
        "call_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable identifier to correlate a tool call with its result."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Tool name."
        },
        "args": {
          "type": "object",
          "description": "Structured tool arguments. Tool-specific validation is handled at the application layer."
        },
        "label": {
          "type": "string",
          "description": "Optional user-facing label for this tool step."
        }
      }
    },

    "ToolResultData": {
      "type": "object",
      "additionalProperties": false,
      "required": ["call_id", "status"],
      "properties": {
        "call_id": {
          "type": "string",
          "minLength": 1,
          "description": "Must reference a tool_call.call_id (enforced partially via co-occurrence rules)."
        },
        "status": {
          "type": "string",
          "enum": ["ok", "error"],
          "description": "Execution outcome."
        },
        "result": {
          "description": "Tool output payload (present when status=ok). Can include artifact_id for external data.",
          "type": ["object", "array", "string", "number", "boolean", "null"]
        },
        "error": {
          "description": "Serialized error payload (present when status=error).",
          "type": "object",
          "additionalProperties": true
        },
        "label": {
          "type": "string",
          "description": "Optional user-facing label for this tool step."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "ok" } }, "required": ["status"] },
          "then": { "required": ["result"] }
        },
        {
          "if": { "properties": { "status": { "const": "error" } }, "required": ["status"] },
          "then": { "required": ["error"] }
        }
      ]
    },


    "Meta": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "client_msg_id": {
          "type": "string",
          "description": "Client-generated UUID for idempotency."
        },
        "request_id": {
          "type": "string",
          "description": "Backend request identifier."
        },
        "trace_id": {
          "type": "string",
          "description": "Distributed tracing identifier."
        },
        "model": {
          "type": "string",
          "description": "Model identifier used to generate assistant content."
        },
        "tokens": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "input": { "type": "integer", "minimum": 0, "description": "Input token count." },
            "output": { "type": "integer", "minimum": 0, "description": "Output token count." }
          }
        }
      }
    }
  }
}
